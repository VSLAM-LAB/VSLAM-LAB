# Smoke test for 'calibration: anycalib' (Datasets/extra-files/run_anycalib.py): the baseline is
# handed a calibration_exp.yaml whose intrinsics come from <sequence>/anycalib/calibration.yaml
# (generated on first use by 'pixi run calib-inference').
test_exp_anycalib_allfeature-dev-mono:
  Config: config_debug.yaml
  NumRuns: 1
  Parameters: {verbose: 1, mode: mono, calibration: anycalib}
  Module: allfeature-dev
